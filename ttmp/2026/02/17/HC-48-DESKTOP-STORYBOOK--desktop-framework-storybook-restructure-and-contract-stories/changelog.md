# Changelog

## 2026-02-17

- Initial workspace created
- Implemented Workstream G hard cutover in commit `c662195`: enforced new engine taxonomy (`Engine/Shell`, `Engine/Widgets`, `Engine/PluginRuntime`), migrated story titles, and added desktop contract/perf harness stories.
- Validation: `npm run storybook:check`, `npm run test -w packages/engine`, and `npm run typecheck` passed after changes.
