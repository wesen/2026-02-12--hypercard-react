{
  "name": "hypercard-react",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/inventory",
    "launcher:frontend:build": "npm run build -w apps/os-launcher",
    "launcher:ui:sync": "bash ./scripts/sync-launcher-ui.sh",
    "launcher:binary:build": "npm run launcher:frontend:build && npm run launcher:ui:sync && bash ./scripts/build-go-go-os-launcher.sh",
    "launcher:smoke": "bash ./scripts/smoke-go-go-os-launcher.sh",
    "build": "npm run build -w packages/engine && npm run build -w packages/confirm-runtime && npm run build -w packages/desktop-os && npm run build -w apps/inventory && npm run build -w apps/todo && npm run build -w apps/crm && npm run build -w apps/book-tracker-debug && npm run build -w apps/os-launcher",
    "storybook": "npm run storybook -w apps/inventory",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --build",
    "test": "npm run storybook:check && npm run test -w packages/engine && npm run test -w packages/desktop-os && npm run test -w apps/os-launcher",
    "build-storybook": "npm run build-storybook -w apps/inventory",
    "storybook:check": "node scripts/storybook/check-taxonomy.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.15",
    "@storybook/addon-a11y": "^10.2.10",
    "@storybook/addon-docs": "^10.2.10",
    "@storybook/addon-onboarding": "^10.2.10",
    "@storybook/addon-vitest": "^10.2.10",
    "@storybook/react": "^10.2.10",
    "@storybook/react-vite": "^10.2.10",
    "storybook": "^10.2.10",
    "vitest": "^4.0.18"
  }
}
